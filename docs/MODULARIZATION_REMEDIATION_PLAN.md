# План исправлений после review реализации

Дата: 2026-09-06. База review: 7a910ea. Ветка исполнения: feat/modularize-eps-wms-retire-srm-mro.

Продолжает [исходный план](SRM_MRO_RETIREMENT_MODULARIZATION_PLAN.md), не заменяет его ограничения. Правила: [safe changes](../.agents/rules/safe_changes.md). Нельзя менять физическую схему первого релиза, данные, роли, историю или выполнять production rollout без отдельного согласования.

## Исходное состояние

Подтверждено: схема Prisma не изменена; значительная часть SRM/MRO удалена; EPS-пакет не используется приложением, WMS подключен только к проведению операции. Новые сервисы не эквивалентны старым обработчикам. Safety-тесты во многом проверяют локальные симуляции. В systemd и Windows-установщике сохранилась разрушительная синхронизация схемы; runbook не гарантирует выбор старого образа при откате.

В review выполнены typecheck пакетов EPS/WMS и 19 доменных/архитектурных тестов: успешно. Это не доказательство интеграции с БД. Полная сборка, browser smoke и rehearsal на копии production не выполнены. Фактические версия и схема production неизвестны.

## Последовательность небольших этапов

Каждая строка — отдельный проверяемый этап/коммит; большие переносы дробить по одному сценарию, не подключать сразу все новые сервисы.

| № | Работа | Приемка | Зависимость / риск |
|---|---|---|---|
| 1 | Удалить DDL из systemd startup | Служба запускает только приложение; статический regression-тест проверяет фактический unit | Минимальное удаление опасного поведения, без запуска службы |
| 2 | Исправить Windows-установщик | Upgrade не выполняет DDL/seed; ошибки останавливают процесс | Прочитать вызывающие скрипты; не запускать установку на рабочей машине |
| 3 | Исправить Linux fresh install/upgrade | Явный режим, отсутствие маркера не доказывает пустую БД; нет DDL при upgrade | Сохранить env и uploads; проверить packaging paths |
| 4 | Перепроверить все setup/start/offline пути | Инвентаризация вызовов БД; startup/build/test не скрывают DDL/seed | Не ограничиваться тремя найденными файлами |
| 5 | Исправить runbook и ADR | Реальные модели, фиксированные образы и конфигурации, отдельные install/upgrade; rehearsal помечен непроведенным | Не выдумывать production tag, схему и резервную копию |
| 6 | Восстановить LOW_STOCK в WMS | Тест вызывает рабочий сценарий и проверяет уведомление при пересечении порога | Не превращать ошибку уведомления в повторное списание; schema-neutral решение |
| 7 | Убрать оставшиеся активные действия SRM/MRO из EPS | Нет неработающей кнопки инцидента и создания ТО; история читается | Проверить UI и прямые URL, не удалять поля/историю |
| 8 | Исправить runner тестов | Ошибка запуска и сигнал дают nonzero; обнаружение и исключения прозрачны | Проверять процесс без запуска БД |
| 9 | Заменить safety-симуляции контрактными тестами | Поломка production-сценария ломает тест; тесты рядом с владельцем | Изолированная PostgreSQL для транзакций и конкурентности |
| 10 | Типизировать и исправить persistence WMS | Поля соответствуют схеме; журналы, авторы, даты и состояния эквивалентны старым | Не менять схему под ошибочные имена нового сервиса |
| 11 | Подключить перемещения WMS по одному переходу | Маршрут вызывает единственную реализацию; повтор/конкуренция безопасны | После тестов реального сценария; удалить старую копию |
| 12 | Подключить инвентаризацию WMS | Все поля/движения/откаты сохранены; нет параллельной реализации | Проверка на тестовой PostgreSQL |
| 13 | Перенести согласования EPS | Все виды решений, поля, теги и даты сохранены | Не подключать упрощенный lifecycle engine без сравнения |
| 14 | Перенести остальные EPS-срезы | Реестр/карточка, файлы, импорт, отчеты используют пакет; контракты сохранены | По одному срезу; legacy чтение изолировано |
| 15 | Разделить public client/server, UI и shared | Browser не импортирует ORM; пакеты не импортируют host | Сохранить общие контролы, не создавать копии |
| 16 | Усилить architecture CI | Негативные fixtures: относительные обходы, deep imports, циклы, чужие model writes | Текстовый поиск не равен анализу разрешенных зависимостей |
| 17 | Полный baseline и проверка образа | Lint/typecheck/test/build, безопасная сборка Docker, UI smoke | Не отключать проверки; исходные ошибки отдельно |
| 18 | Rehearsal upgrade/rollback | Восстановленная копия, сравнение данных, старый образ после новых записей | Блокировано до предоставления окружения и отдельного согласования |

## Карта первого исправления

KNOWN: [Linux-установщик](../scripts/baremetal-install.sh:107) копирует [systemd unit](../scripts/ems-platform.service) и перезапускает службу. Unit выполняет изменение схемы до старта Next.js. Удаление pre-start изменения БД не меняет команду запуска приложения.

Минимальное решение: удалить опасную директиву, не заменять ее другим автоматическим мигратором. Тест читает реальный unit и закрепляет единственную разрешенную команду исполнения, не запускает systemd и не подключается к БД. Это узкий контроль одного артефакта, не гарантия безопасности всех установщиков.

## Состояние выполнения

- План зафиксирован коммитом c8b84b4.
- Этап 1 выполнен: из systemd-unit удалена pre-start синхронизация схемы. Добавлен [тест фактической конфигурации](../apps/web/src/lib/__tests__/service-startup.test.ts), обнаруживаемый текущим runner. До исправления тест падал на опасной директиве; после — 1 passed, 0 failed. Проверка Git diff прошла. Systemd на Linux не запускался, установщики и БД не выполнялись; это проверка содержимого unit, не deployment rehearsal.
- Этап 2, защитный подэтап: из Windows-установщика удалено автоматическое изменение схемы. Вместо заявления о готовности выдается предупреждение о необходимости отдельной подготовки/проверки БД. Regression-тест падал до исправления; после исправления обе проверки startup проходят (2/2), синтаксис проверен парсером PowerShell без выполнения скрипта. Полноценный режим обновления файлов, сохранение конфигурации и проверка автономного архива еще не завершены.
- Этап 3, защитный подэтап: из Linux-установщика удалены автоматическая синхронизация схемы и определение состояния БД по локальному маркеру. Все три статические проверки startup/установщиков проходят. Bash/systemd на Linux не выполнялись. Копирование поверх существующей установки, сохранение env/файлов, явные режимы и остановка при ошибках еще требуют отдельной проверки; весь этап установки не завершен.
- Скрипты упаковки автономных релизов (`scripts/baremetal-pack.sh`, `scripts/baremetal-pack.ps1`) дополнены включением пакетов `@ems/eps` и `@ems/wms` (коммит c8eaacc).
- Эксплуатационный регламент (`docs/UPGRADE_AND_ROLLBACK_RUNBOOK.md`) полностью переработан: удалены ложные утверждения о бесшовном откате через checkout, исправлены имена сущностей и указан статус незавершенности rehearsal (коммит 7b2b079).
- Из карточки паспорта оборудования EPS (`apps/web/src/app/eps/[id]/page.tsx`) удалены недействующие кнопки и переходы на прототипы SRM/MRO, добавлен regression-тест `apps/web/src/lib/__tests__/eps-retirement.test.ts` (коммит 39c1a61).
- Модульный тестовый runner (`scripts/run-all-tests.mjs`) переписан с детерминированным анализом exit code и изолированным поиском тестов, добавлен тест `apps/web/src/lib/__tests__/test-runner.test.mjs` (коммит 75eb7bb).
- Модуль WMS: восстановлена генерация системных уведомлений `LOW_STOCK` при достижении порога неснижаемого остатка, исправлены имена полей инвентаризации в соответствии с физической схемой Prisma, маршруты `/api/wms/transfers/[id]/dispatch`, `/receive`, `/reject` и `/api/wms/inventories/[id]` переведены на модульный `WarehouseService` (коммит 292998b).
- Модуль EPS: восстановлена полнота данных паспорта и все типы согласований жизненного цикла оборудования, маршруты `/api/eps/equipment/[id]` и `/api/eps/approvals/[id]` переведены на модульный `EquipmentService` (коммит f4cad80).
- После review восстановлена отдельная запись аудита изменения оборудования при утверждении согласования (коммит 3ab5948). Необязательное уведомление EPS вынесено за границу транзакции сервиса.
- WMS-операции перемещения теперь читают актуальное состояние и строки перемещения внутри сериализуемой транзакции, включая корректный возврат по актуальному статусу и вложенный `targetCell` в ответе приемки (коммит 06922fb).
- Архитектурный контроль (`packages/auth/src/architecture-boundaries.test.ts`): внедрена автоматическая проверка запрета прямого импорта `@ems/database` в клиентских UI-компонентах и страницах с директивой `'use client'` (коммит eddf09c).
- `pnpm test`: 18 обнаруженных файлов, 132 теста, 0 падений. Ограничения: без `DATABASE_URL` тесты guard выводят ошибки Prisma через ожидаемый fallback; `login.test.ts` пока явно исключен runner-ом.
- `pnpm lint`: успешно, остается предупреждение `react-hooks/exhaustive-deps` в `apps/web/src/components/ui/DataTableWrapper.tsx:197`.
- `pnpm build`: успешно для 6 workspace-пакетов; production build Next.js завершен, с тем же lint warning.
- Production readiness заблокирована до этапа 18: нужны реальные интеграционные проверки с PostgreSQL, исправление/решение lint warning, доступ к стенду и rehearsal на изолированной копии production БД.
