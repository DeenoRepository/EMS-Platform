---
id: K4.9
title: Декомпозировать GET истории аудита EPS
status: active
phase: K
priority: P2
risk: medium
skills: [senior-backend, zero-hallucination-coder]
opened: 2026-08-30
closed: null
commits: []
gates: [test, lint, tsc, check:quality, check:docs]
parent: K4
---

# K4.9 — Декомпозировать GET истории аудита EPS

## Problem

Повторное измерение после K4.8 показывает, что следующий проверенный business
candidate — [`GET()`](../../apps/web/src/app/api/eps/history/route.ts:11) с
`cx 23` и 61 строкой по границам quality checker. Handler одновременно
разбирает query-параметры, строит Prisma filter, загружает audit logs and
statistics, enriches equipment references and formats the response.

Численно более высокие результаты для
[`WmsOperationWizardDialog`](../../apps/web/src/components/wms/WmsOperationWizardDialog.tsx),
[`parseDateSafe()`](../../apps/web/src/app/api/eps/equipment/[id]/route.ts:148) и
presentation boundaries остаются false-positive или out of scope для этой
bounded stage.

## Verified map

- [KNOWN] Единственный route handler для scope —
  [`apps/web/src/app/api/eps/history/route.ts`](../../apps/web/src/app/api/eps/history/route.ts).
- [KNOWN] GET response contract contains `success`, `data.items`, pagination and
  `stats`; no POST handler is in this file.
- [KNOWN] `AuditAction`, Prisma types and existing auth/rate-limit/logging APIs
  are already imported from installed workspace packages.
- [KNOWN] Authorization, rate limiting, Prisma query shape, pagination,
  enrichment behavior, response mapping, permission semantics and error
  handling must remain unchanged.
- [KNOWN] The existing test runner discovers tests in
  `apps/web/src/lib/__tests__` and package tests; this helper test must be
  placed in a discovered location or the runner must explicitly include it.

## Scope

Extract only pure query parsing/filter construction, equipment-reference
collection/enrichment mapping, and audit statistics needed by GET. Preserve
Prisma query shape, permission behavior, response contract and error handling.
Do not change UI, unrelated API routes, security behavior, K6, or other K4
candidates.

One logical remediation stage: EPS audit history GET query/enrichment/statistics.

## Definition of Done

- [ ] Real [`GET()`](../../apps/web/src/app/api/eps/history/route.ts:11)
  complexity is reduced to `<= 10`, or a remaining orchestration boundary is
  explicitly documented.
- [ ] Focused pure-helper tests cover query defaults/clamping, filters,
  equipment references and all audit statistics branches.
- [ ] API response, permissions, Prisma semantics, pagination and error behavior
  are unchanged.
- [ ] Targeted tests, full tests, lint, web tsc, quality and docs checks pass.
- [ ] Story is moved to `plans/done/2026-08/` only after the successful
  implementation commit.

## Ponytail check

Rung 1 does not apply: the EPS audit history endpoint serves the active audit
workflow. Rungs 2–4: `URLSearchParams`, plain objects, `Set`, `Map` and native
array methods are sufficient; no new dependency or framework abstraction is
justified. Rung 6: minimum implementation is one neighboring pure helper module
plus focused tests, with no route contract redesign.

## Result

Implementation pending.
