---
id: K4.2
title: Декомпозировать сохранение складов WMS
status: done
phase: K
priority: P2
risk: medium
skills: [senior-frontend, zero-hallucination-coder]
opened: 2026-08-30
closed: 2026-08-30
commits: [5ef7e08]
gates: [test, lint, tsc, check:quality]
parent: K4
---

# K4.2 — Декомпозировать сохранение складов WMS

## Problem

`handleSubmit` в [`apps/web/src/app/wms/warehouses/page.tsx`](../../../apps/web/src/app/wms/warehouses/page.tsx:168) имеет реальную цикломатическую сложность `cx 16` и 65 строк по текущему quality inspection. Его pure request builder уже вынесен в [`warehouse-submit.ts`](../../../apps/web/src/app/wms/warehouses/warehouse-submit.ts:14), но orchestration HTTP response mapping остаётся внутри page-компонента.

## Scope

- Вынести только response/status mapping для существующего warehouse submit flow в соседний pure helper.
- Сохранить validation, request payload, HTTP method/URL, success/error/network messages, modal close, refresh и submitting-state semantics.
- Добавить unit-тесты на успешное создание/обновление, API error и network error mapping.
- Не менять API routes, Prisma semantics, permissions, rate limiting, UI layout или unrelated K4/K5/K6 work.

## Steps

1. Зафиксировать `handleSubmit` consumers and existing request-builder contract.
2. Вынести минимальный helper для response outcome mapping with callbacks.
3. Cover all response branches with focused unit tests.
4. Run targeted tests, full tests, lint, web tsc and quality baseline.

## Definition of Done

- [x] Real `handleSubmit` complexity in the warehouses page is `<= 10` or the remaining complexity is documented.
- [x] Existing validation, fetch, notifications, modal, refresh and loading behavior is unchanged.
- [x] Focused tests cover success, API error and network error branches.
- [x] Full gates green: test, lint, tsc, check:quality.
- [x] Story is closed, indexed, and committed with one Conventional Commit.

## Result

- Extracted request execution and HTTP response mapping from the page handler into [`warehouse-submit.ts`](../../../apps/web/src/app/wms/warehouses/warehouse-submit.ts:38).
- Added focused coverage in [`warehouse-submit.test.ts`](../../../apps/web/src/app/wms/warehouses/warehouse-submit.test.ts:7) for create/update success and API error fallbacks.
- Re-measured the page handler at `cx 12`; the remaining two branches are the validation guard and the `try/catch/finally` orchestration boundary, so the target is documented as an exception for this bounded stage.
- Targeted test, full test (`165` passing), lint, web TypeScript, and quality baseline all passed.
- Closed in implementation commit `5ef7e08`; this ledger closeout records the already verified stage.
